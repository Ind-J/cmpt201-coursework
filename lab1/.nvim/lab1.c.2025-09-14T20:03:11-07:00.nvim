#include <stdio.h>
int main() 
{ 
  printf("Hello World\n"); 
  char *buff = NULL;
  char size = 0;
  ssize_t num_char = getline(&buff, &size, stdin);
  if (num_char == -1) {
    perror("getline failed");
    exit(EXIT_FAILURE);
  }
  



  free(buff);
}

